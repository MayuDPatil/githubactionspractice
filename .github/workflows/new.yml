name: python lint

on:
    # push:
    workflow_dispatch:

jobs:
    #jobname
    validate:
        runs-on: ubuntu-latest

        steps:
          - name: checkout code
            uses: actions/checkout@v4

          - name: setup python
            uses: actions/setup-python@v6 
            with:
                python-version: '3.13' 
        
    
